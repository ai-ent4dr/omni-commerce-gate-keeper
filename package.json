{
  "name": "omni-commerce-gatekeeper",
  "version": "6.0.0",
  "description": "Autonomous AP2 Gated Settlement Engine",
  "main": "main.py",
  "scripts": {
    "build": "pip install -r requirements.txt",
    "start": "uvicorn main:app --host 0.0.0.0 --port $PORT"
  }
}
